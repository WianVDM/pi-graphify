{
  "$schema": "https://raw.githubusercontent.com/fallow-rs/fallow/main/schema.json",
  "entry": ["extensions/index.ts"],
  "ignoreDependencies": ["chokidar"],
  "health": {
    "thresholdOverrides": [
      {
        "files": ["src/backends/cli.ts"],
        "functions": ["buildArgs"],
        "maxCrap": 600,
        "reason": "CLI flag builder with additive conditional flags per Graphify operation; will be covered by tests once the test framework is introduced."
      },
      {
        "files": ["src/errors.ts"],
        "functions": ["codeForOperation"],
        "maxCrap": 120,
        "reason": "Simple operation-to-error-code mapping switch; high CRAP is due to lack of test coverage, not structural complexity."
      }
    ]
  }
}
